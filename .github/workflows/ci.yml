name: CICD

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: make file
        run: |
          expr 90 + 2 > docs/math.txt

      - name: listing
        run: ls docs
